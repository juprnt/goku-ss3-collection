# Copier ce fichier en .env et ajuster si besoin.
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen3.6:35b-mlx

# Mêmes valeurs que dans index.html (clé anon publique). Aucune clé service_role ici :
# le serveur agit toujours avec le jeton de l'utilisateur connecté, donc RLS s'applique.
SUPABASE_URL=https://vtdohksscretlbvhsgfo.supabase.co
SUPABASE_ANON_KEY=

# Origines autorisées à appeler le serveur depuis un navigateur / l'app Capacitor.
ALLOWED_ORIGINS=https://goku-ss3-collection.vercel.app,http://localhost:8787,http://127.0.0.1:8787,capacitor://localhost,https://localhost,http://localhost

PORT=8787
